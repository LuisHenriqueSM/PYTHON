mport socket

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
host ="10.163.155.106"
port =3003
s.connect((host,port))

def ts(str):
   s.send('I–Ò   (                           ¶iý.'.encode()) 
   data = ''
   data = s.recv(1024).decode()
   print (data)

while 2:
   r = input('enter')
   ts(s)

s.close ()
    
    
